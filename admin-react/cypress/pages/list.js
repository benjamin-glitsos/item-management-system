export default class ListPage {
    // TODO: use clicking on the sidebar item from home instead of this. Extend home class and use this method with an argument? Overwrite method?
    // visit(slug) {
    //     cy.visit(`/${slug}`);
    // }
}
