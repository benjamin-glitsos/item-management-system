import Error from "%/utilities/error";

export default new Error(
    "no_new_data_to_submit",
    "No new data to submit",
    "No values in the form are different to the data that is already present on the server, so there is no new data to submit."
);
