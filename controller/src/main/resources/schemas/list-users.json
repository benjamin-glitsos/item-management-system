{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "List Users",
    "description": "List the users who are registered to access this system.",
    "type": "object",
    "properties": {
        "page_number": {
            "description": "The page of items to return.",
            "type": "integer",
            "minimum": 0,
            "default": 0
        },
        "page_length": {
            "description": "The number of items per page.",
            "type": "integer",
            "enum": [10, 25, 50, 100],
            "default": 10
        }
    }
}
