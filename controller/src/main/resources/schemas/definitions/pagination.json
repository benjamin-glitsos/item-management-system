{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Pagination",
    "description": "Definitions for use in the pagination of lists of results.",
    "definitions": {
        "page_number": {
            "description": "The page of items to return.",
            "type": "integer",
            "minimum": 1
        },
        "page_length": {
            "description": "The number of items per page.",
            "type": "integer",
            "enum": [10, 25, 50, 100]
        }
    }
}
