{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Edit Users",
    "type": "object",
    "properties": {
        "username": {
            "type": "string",
            "maxLength": 20,
            "pattern": "^[-_a-zA-Z0-9]*$"
        },
        "email_address": {
            "type": "string",
            "maxLength": 50,
            "format": "email"
        },
        "first_name": {
            "type": "string",
            "maxLength": 50,
            "pattern": "^(?! )[A-Za-z0-9 ]*(?<! )$"
        },
        "last_name": {
            "type": "string",
            "maxLength": 70,
            "pattern": "^(?! )[A-Za-z0-9 ]*(?<! )$"
        },
        "other_names": {
            "type": "string",
            "maxLength": 120,
            "pattern": "^(?! )[A-Za-z0-9 ]*(?<! )$"
        },
        "password": {
            "type": "string",
            "maxLength": 40,
            "pattern": "^[^\\s]+(s+[^\\s]+)*$"
        },
        "additional_notes": {
            "type": "string",
            "maxLength": 1048576,
            "pattern": "[\\s\\S]*\\S[\\s\\S]*"
        }
    },
    "minProperties": 1
}
