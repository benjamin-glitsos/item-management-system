{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Delete or Restore Items",
    "type": "object",
    "properties": {
        "method": {
            "type": "string",
            "enum": ["soft", "hard", "restore"]
        },
        "keys": {
            "type": "array",
            "minItems": 1,
            "maxItems": 100,
            "uniqueItems": true,
            "items": {
                "type": "string",
                "minLength": 1,
                "maxLength": 255,
                "pattern": "^[^\\s]+(s+[^\\s]+)*$"
            }
        }
    },
    "required": ["method", "keys"]
}
